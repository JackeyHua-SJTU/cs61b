import org.junit.Test;
import static org.junit.Assert.*;

public class test {
    @Test
    public void testFlik(){
        boolean test = Flik.isSameNumber(128, 128);
        assertTrue(test);
    }
}
